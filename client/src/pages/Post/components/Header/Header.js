//import styled components
import { HeaderContainer, HeaderSection, HeaderLogo } from './Header-Styled';

function Header() {
    return (
        <HeaderContainer>
            <HeaderSection>
                <HeaderLogo>Loka</HeaderLogo>
            </HeaderSection>
        </HeaderContainer>
    );
};

export default Header;